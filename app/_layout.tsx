import { Stack } from "expo-router";

const Layout = () => {
	return <Stack />;
};

export default Layout;

/* const Layout = Stack({
	Home: {
		screen: App,
		path: "/",
	},
	About: {
		screen: AboutScreen,
		path: "/about",
	},
}); */
